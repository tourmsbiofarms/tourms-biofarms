export const metadata = {
  title: "Checkout - Tourms Biofarms Ventures",
  description: "Complete your order. Pay on delivery — no online payment required.",
};

export default function CheckoutLayout({ children }) {
  return children;
}